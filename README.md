## Pour start le projet en DDEV (Project root)
ddev config (Configure your new project domain)
ddev start
composer setup

## Pour install les dépendances (Project root)
yarn install (Setup vendors and node modules for root and theme)
yarn dev (Starts Vite instantly!)